<h2>Providers web service</h2>

<h3>Synopsis</h3>
<p>Get a list of available providers</p>

<h3>Request format</h3>
<p><code><%= providers_url %></code></p>

<h3>Response format</h3>
<pre class="radius pretty_json">
  <%= render template: "providers/index", formats: [:json], locals: { providers: @providers.take(2) } %>
</pre>

<h3>Providers</h3>
<% if @providers.any? %>
<ul>
  <% @providers.each do |provider| %>
  <li><%= link_to(provider.name, provider_path(provider)) %></li>
  <% end %>
</ul>
<% end %>